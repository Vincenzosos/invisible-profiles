\chaptertitlepage{Methodology}{Data, variable selection, preprocessing, factor structure, clustering pipeline}
\label{ch:ch3}

\section{Empirical strategy}
\label{sec:ch3-empirical-strategy}

The empirical strategy of this thesis rests on three commitments. The first is methodological: the multidimensional structure of late-life experience is recovered from a data-driven segmentation on a harmonised analytical set, not from a manual partition of cross-tabulations. The second is comparative: two segmentations are estimated independently in Italy and Sweden, the leading Mediterranean-familistic and Nordic-universalistic cases, and compared on structurally meaningful axes. The third is conceptual: the analytical set includes both objective dimensions (health, economic resources, digital and social integration, cognitive function) and subjective dimensions (CASP-12, loneliness, hope for the future, interest, expected survival), with the latter entering as constituent inputs rather than external validation outcomes.

\begin{figure}[!htbp]
  \centering
  \begin{tikzpicture}[
    node distance=6mm and 6mm,
    box/.style={draw, rounded corners=2pt, align=center, minimum width=3.0cm, minimum height=0.85cm, font=\small\sffamily, inner sep=4pt},
    raw/.style={box, fill=gray!8},
    pre/.style={box, fill=blue!4},
    seg/.style={box, fill=blue!10},
    val/.style={box, fill=blue!4},
    rob/.style={box, fill=gray!8},
    arr/.style={-{Stealth[length=2.4mm]}, thick, gray!70!black}
  ]
    \node[raw] (data) {SHARE Wave~9 sample\\\footnotesize 3{,}623 IT \,/\, 2{,}428 SE};
    \node[pre, below=of data] (sample) {Sample restriction\\\footnotesize age\,$\geq$\,65, no proxies; \\\footnotesize listwise + Mahalanobis};
    \node[pre, below=of sample] (std) {Within-country\\\footnotesize rank + z-standardisation\\\footnotesize $n = 2{,}378$ IT, $1{,}787$ SE};
    \node[pre, below left=6mm and 6mm of std] (fa) {Factor analysis\\\footnotesize 6 factors, varimax\\\footnotesize KMO 0.83 IT, 0.81 SE};
    \node[seg, below right=6mm and 6mm of std] (lca) {LCA triangulation\\\footnotesize same $k$, modal\\\footnotesize class assignment};
    \node[seg, below=8mm of fa] (km) {K-means\\\footnotesize $k = 5$ IT, $k = 6$ SE\\\footnotesize seed 42, 100 starts};
    \node[val, below=8mm of km] (cc) {Cross-country\\\footnotesize Hungarian matching\\\footnotesize ($d \leq 2.0$ threshold)};
    \node[rob, right=18mm of cc] (rob) {Robustness battery (Ch.~7)\\\footnotesize imputation, internal validity, external,\\\footnotesize cross-method, cross-wave W8\,$\leftrightarrow$\,W9};
    %
    \draw[arr] (data) -- (sample);
    \draw[arr] (sample) -- (std);
    \draw[arr] (std.south) -- ++(0,-2mm) -| (fa.north);
    \draw[arr] (std.south) -- ++(0,-2mm) -| (lca.north);
    \draw[arr] (fa) -- (km);
    \draw[arr] (km) -- (cc);
    \draw[arr] (km.east) -| (rob.north);
    \draw[arr] (lca) -- (rob);
  \end{tikzpicture}
  \caption[Analytical pipeline]{Analytical pipeline. The SHARE Wave~9 sample passes through restriction, listwise deletion with Mahalanobis screening, and within-country rank-and-z standardisation; the harmonised matrix then feeds two parallel paths. The interpretive path runs factor analysis as a pre-clustering diagnostic and feeds the K-means estimation; the triangulation path estimates a Latent Class Analysis on the same matrix for cross-method validation. K-means supplies the centroids for the cross-country Hungarian matching of Chapter~\ref{ch:ch6}, and both K-means and LCA feed the robustness battery of Chapter~\ref{ch:ch7}.}
  \label{fig:ch3-pipeline}
\end{figure}

This chapter sets out the operational implementation of the pipeline summarised in Figure~\ref{fig:ch3-pipeline}. Internal validity, multi-algorithm comparison, and cross-wave stability are reported in Chapter~\ref{ch:ch7}.

\section{Data and analytical sample}
\label{sec:ch3-data}

The Survey of Health, Ageing and Retirement in Europe (SHARE) is a multidisciplinary, cross-national panel of individuals aged 50 and older resident in 28 European countries \citep{borschsupan2013}, harmonised ex ante on questionnaire content and ex post on variable definitions. Wave~9 was fielded between June 2021 and August 2022, covering the period that follows the most acute phase of the COVID-19 pandemic. The wave is the most recent fully released SHARE wave at the time of writing. The pre-pandemic Wave~8 (2019--2020) is used in \S\ref{sec:ch7-cross-wave} as the comparator for the cross-wave robustness analysis.

Italy and Sweden are selected on theoretical grounds. They occupy opposite ends of \citet{espingandersen1990} typology of welfare regimes. Italy is the leading Mediterranean-familistic case (high reliance on intra-family care, a fragmented social-protection system, a predominance of cash transfers in elderly care); Sweden is the prototypical Nordic-universalistic case (extensive public long-term care, dense community-based social services, largely de-familised elderly care). The two also differ markedly in digital adoption among older adults: \citet{eurostat2022} reports 51\% internet use in the past three months for Italian residents aged 65--74, against 96\% in Sweden.

The analytical sample is restricted to respondents aged 65 or older at the date of interview, with proxy interviews excluded. The SHARE multiple-imputation procedure is applied to income, wealth and pension variables, and the analytical sample uses the first imputation (implicat = 1). Table~\ref{tab:ch3-sample-sizes} reports the sample derivation. The Italian first-imputation sample (all ages) contains 3,623 individuals; restriction to the over-65 population yields 2,804, listwise deletion on the 31 analytical variables retains 2,431 (a 13.3\% loss), and Mahalanobis-distance outlier screening at $p = 0.001$ removes a further 53 (2.2\%), for a final analytical sample of 2,378. The Swedish sequence is parallel: 2,428 individuals, 2,045 over-65, 1,831 after listwise deletion (a 10.5\% loss), and 1,787 after Mahalanobis screening (44 removed, 2.4\%). In both countries the listwise deletion is the larger source of attrition and is concentrated in the subjective and social block (self-rated survival, CASP-12, the social-integration index, and the cultural-participation items), which carries the highest item-level missingness; the Mahalanobis outlier exclusion is modest and nearly symmetric across the two countries (2.2\% Italy, 2.4\% Sweden), as visualised in \S\ref{sec:ch3-preprocessing} below (Figure~\ref{fig:ch3-mahalanobis}). These counts are taken on the SHARE first-imputation generated-variables sample (\texttt{implicat} = 1), the subset of the Wave~9 release carrying the imputed income, wealth and pension variables, from which the analytical funnel is drawn; the broader Wave~9 country release is larger but is not the operative base for the imputed monetary inputs the segmentation requires.

All data used here are de-identified SHARE microdata, released under the survey's research-use conditions and collected under its harmonised ethics and informed-consent governance \citep{borschsupan2013}. No individual is identifiable in the analytical sample or in any reported result: the substantive findings are stated at the level of cluster centroids and aggregate shares (\S\ref{sec:ch3-kmeans}).

\begin{table}[H]
  \centering
  \caption[Analytical sample by country and stage]{Analytical sample, by country and stage}
  \label{tab:ch3-sample-sizes}
  \begin{tabular}{lrr}
    \toprule
    \textbf{Stage} & \textbf{Italy} & \textbf{Sweden} \\
    \midrule
    Wave~9 sample, all ages (first imputation) & 3{,}623 & 2{,}428 \\
    Aged 65 or older at first imputation & 2{,}804 & 2{,}045 \\
    After listwise deletion on 31 variables & 2{,}431 & 1{,}831 \\
    After Mahalanobis screening ($p = 0.001$) & 2{,}378 & 1{,}787 \\
    \textbf{Final analytical sample} & \textbf{2{,}378} & \textbf{1{,}787} \\
    Active analytical variables & 29 & 31 \\
    Number of clusters retained ($k$) & 5 & 6 \\
    \bottomrule
  \end{tabular}
  \par\medskip
  \begin{minipage}{\linewidth}
    \footnotesize\textit{Note.} The number of clusters is selected by joint reading of the Duda--Hart pseudo-$T^2$ statistic, the within-cluster sum of squares scree, and substantive interpretability (\S\ref{sec:ch3-kmeans}).
  \end{minipage}
\end{table}

\section{Variable inventory}
\label{sec:ch3-variables}

Thirty-one analytical variables span five conceptual dimensions, following the SHARE conceptual framework on the multidimensional measurement of late-life experience \citep{wahrendorfsiegrist2010, jivraj2020}. Where multiple SHARE items operationalise the same construct (for example, mobility limitations, ADL and IADL for functional health), all candidate items are retained and dimensionality reduction is delegated to the preprocessing and to the clustering algorithm itself; the full inventory is listed in Table~\ref{tab:ch3-variables}. Income, wealth and pension are imputed via the SHARE multiple-imputation procedure. Two cultural-engagement binary indicators (\texttt{ac035d4}, \texttt{ac035d7}) have zero variance in the Italian analytical sample and are dropped at the variance-screening stage, accounting for the 29-versus-31 active-variable asymmetry across countries (\S\ref{sec:ch3-kmeans}). The cognitive items are near-complete in both samples; the principal source of listwise loss reported in \S\ref{sec:ch3-data} is item-level missingness on the subjective and social-engagement block --- self-rated survival, CASP-12, the social-integration index, and the cultural-participation items --- which carries the highest item-level missingness in both countries.

\begin{table}[H]
  \centering
  \caption[Analytical variable inventory]{Analytical variable inventory by conceptual dimension}
  \label{tab:ch3-variables}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{>{\raggedright\arraybackslash}p{4.0cm}>{\raggedright\arraybackslash}p{9.5cm}}
    \toprule
    \textbf{Dimension} & \textbf{Variables (SHARE codename)} \\
    \midrule
    Physical and functional health (8) & sphus, chronic, adl, iadl, mobility, bmi, phinact, eurod \\
    Economic resources (5) & log\_thinc, log\_hnetw, ypen1, home\_own, fdistress \\
    Digital and social integration (10) & internet, sn\_size\_w9, social\_integration, ac035d1, ac035d5, ac035d8, sp002\_, sp008\_, plus two country-specific items \\
    Cognitive function (3) & fluency, memory, orienti \\
    Subjective well-being (5) & casp, loneliness, hope\_future, interest, expect\_alive \\
    \bottomrule
  \end{tabular}
  \par\medskip
  \begin{minipage}{\linewidth}
    \footnotesize\textit{Note.} Variable codes follow the SHARE Wave~9 release naming convention. Italy retains 29 active variables (ac035d4, ac035d7 dropped); Sweden retains the full 31 items.
  \end{minipage}
\end{table}

\section{Preprocessing pipeline}
\label{sec:ch3-preprocessing}

The preprocessing pipeline operates in four stages --- sample selection, variable selection, listwise deletion with Mahalanobis screening, and scale harmonisation by rank transformation followed by z-standardisation --- feeding the K-means estimation reported in \S\ref{sec:ch3-kmeans}. Each stage is implemented as a numbered R script with fixed seed 42.

Listwise deletion removes a respondent if any of the 31 analytical variables is missing. SHARE imputes income, wealth and pension ex ante \citep{christelis2011}, so the principal source of listwise loss is non-response on cognitive and subjective items. Multiple-imputation pooling across replicates is not used at the segmentation stage: the K-means objective is not invariant to imputation noise, and Rubin-style consensus algorithms for high-dimensional clustering are not yet well established \citep{vermuntmagidson2002}; the analytical sample therefore uses the first imputation (\S\ref{sec:ch3-data}) and the diagnostics of \S\ref{sec:ch7-imputation} quantify the residual sensitivity to the imputation draw. After listwise deletion, the sample is screened for multivariate outliers using Mahalanobis distance, with respondents above the chi-squared critical value at $p = 0.001$ excluded (the resulting losses by country are reported in \S\ref{sec:ch3-data}). The sensitivity of the partition to the $\alpha = 0.001$ threshold is quantified at the profile level in \S\ref{sec:ch7-outlier-robustness}, with the full robustness battery documented in \S\ref{sec:appx-sens-outlier}.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/03_data_methods/fig_mahalanobis_distribution_v2.png}
  \caption[Mahalanobis distance distribution, Italy and Sweden]{Mahalanobis distance distribution, Italy and Sweden. Kernel density on the country-specific standardised analytical matrix; dashed line marks the chi-squared critical value at $p = 0.001$ used for outlier exclusion. Italy loses 53 (2.2\%); Sweden loses 44 (2.4\%) in the right tail.}
  \label{fig:ch3-mahalanobis}
\end{figure}

Continuous and ordinal variables are then rank-transformed within each country, mapping the original distribution onto a uniform rank distribution; binary variables pass through unchanged. The rank-transformed and binary variables are z-standardised within each country, so each enters K-means with mean zero and unit variance. Standardisation is performed at the country level rather than on the pooled sample, to preserve the within-country interpretation of the centroids: a respondent classified ``Connected Active'' in Italy is so classified relative to the Italian distribution, and is then compared in Chapter~6 with the analogous ``Connected Wealthy'' archetype defined relative to the Swedish distribution. The partition's sensitivity to the weighting of the binary indicators, including a Gower-dissimilarity alternative and an explicit binary down-weighting, is examined in \S\ref{sec:appx-sens-gower}.

The SHARE design and post-stratification weights are not applied at the segmentation stage: the segmentation operates unweighted, because the K-means objective is sensitive to weight-induced distortions of the within-cluster geometry on which the choice of $k$ in \S\ref{sec:ch3-kmeans} rests. The substantive findings are accordingly stated as claims about cluster centroids and structural relations rather than probabilistic generalisations to the national over-65 populations. A post-hoc projection using the SHARE Wave~9 design weight (\texttt{cciw\_w9}) confirms that the descriptive cluster shares are stable under re-weighting, with differences uniformly within 3.2 percentage points across all eleven profiles (Appendix~\ref{ch:appx-weighted-shares}); the national-population projection in Figure~\ref{fig:ch4-shares} applies these weighted post-hoc shares to the 2024 Istat over-65 estimate.

\section{Factor structure of the harmonised input space}
\label{sec:ch3-fa}

Before estimating the K-means partition, the preprocessed input space is examined through factor analysis. The purpose is twofold: a sanity check on whether the variables share sufficient common structure to be analysed jointly, and a low-dimensional representation that supports the substantive interpretation of the cluster solution.

The common-factor model assumes that the $p$-dimensional vector of standardised observed variables $\mathbf{X}$ admits the decomposition
\begin{equation}
  \mathbf{X} = \mathbf{\Lambda}\mathbf{F} + \boldsymbol{\epsilon},
  \qquad
  \operatorname{Cov}(\mathbf{X}) = \mathbf{\Lambda}\mathbf{\Lambda}^{\top} + \boldsymbol{\Psi},
\end{equation}
where $\mathbf{\Lambda}$ is the $p \times m$ matrix of factor loadings on $m$ latent common factors $\mathbf{F}$, $\boldsymbol{\Psi}$ is the diagonal matrix of unique variances, and the standard identifying assumptions hold ($\operatorname{E}[\mathbf{F}] = \mathbf{0}$, $\operatorname{Cov}(\mathbf{F}) = \mathbf{I}$, $\operatorname{Cov}(\boldsymbol{\epsilon}) = \boldsymbol{\Psi}$, $\operatorname{Cov}(\mathbf{F},\boldsymbol{\epsilon}) = \mathbf{0}$). Principal Axis factoring estimates $\mathbf{\Lambda}$ iteratively by spectral decomposition of the reduced correlation matrix $\mathbf{R} - \boldsymbol{\Psi}$, replacing the unit diagonal of $\mathbf{R}$ with the current estimate of communalities until convergence. The Italian solution uses this principal-axis estimator; the Swedish solution uses maximum-likelihood extraction, which estimates $\mathbf{\Lambda}$ and $\boldsymbol{\Psi}$ by maximising the Gaussian likelihood of the observed correlation matrix and is adopted because it resolves a Heywood case that principal-axis extraction produces on the social-integration index (\S\ref{sec:appx-sens-fa}).

The Kaiser--Meyer--Olkin (KMO) measure is 0.832 in Italy and 0.808 in Sweden, both well above the 0.700 threshold conventionally taken as ``good'' adequacy \citep{kaiser1974}; the Bartlett test of sphericity \citep{bartlett1950} rejects the null in both countries ($p < 0.001$). The number of factors is selected by joint reading of three criteria: parallel analysis \citep{horn1965}, BIC for $m$ between 2 and 8, and substantive interpretability of the rotated loadings. The country-level BIC decreases across the tested range, reaching its within-range minimum at $m = 8$ (Italy, principal-axis) and $m = 6$--$7$ (Sweden, maximum-likelihood); the parallel-analysis criterion suggests $m = 6$ (Italy) and $m = 8$ (Sweden); substantive interpretability is highest at $m = 6$ in both countries. The six-factor solution is therefore retained on cross-country-comparability and interpretability grounds; it is conservative relative to the BIC minimum in Italy ($m = 8$) and at the lower edge of the BIC range in Sweden ($m = 6$--$7$). The RMSEA at $m = 6$ is 0.054 in Italy and 0.033 in Sweden, against 0.040 and 0.029 at the respective within-range BIC minimum. Figure~\ref{fig:ch3-fa-fit} reports the four fit indices for $m$ between 4 and 8.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/03_data_methods/fig_3_3_fa_fit_comparison_v2.png}
  \caption[Factor-analysis fit indices across $m$]{Factor-analysis fit indices across number of factors. Four indices (RMSR, TLI, RMSEA, BIC) reported for $m = 4$ to 8 factors, separately for Italy (principal-axis) and Sweden (maximum-likelihood). Dashed reference line marks the $m = 6$ design choice.}
  \label{fig:ch3-fa-fit}
\end{figure}

Loadings are rotated by varimax \citep{kaiser1958}. Figure~\ref{fig:ch3-fa-loadings} reports the Italian and Swedish solutions side by side; the six factors carry the substantive labels Factor~1 \emph{physical and functional fragility}, Factor~2 \emph{subjective burden}, Factor~3 \emph{quality of life and engagement}, Factor~4 \emph{social integration}, Factor~5 \emph{household income}, Factor~6 \emph{household wealth}.\footnote{The R \texttt{psych} package outputs the same factors in variance-explained order as PA1, PA5, PA2, PA3, PA4, PA6 respectively. The substantive renumbering used throughout the thesis preserves the conceptual progression from objective fragility to economic resources via subjective and social dimensions.} The Swedish solution recovers the same six-factor structure with broadly similar loadings on the health, social and economic blocks; cross-country differences in the subjective and digital factors are developed in Chapter~6. The six factors jointly explain 38.4\% of the variance in the 29-variable Italian active set.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/03_data_methods/fig_3_3_NEW_v3.png}
  \caption[Factor loadings matrix, Italy and Sweden]{Factor loadings matrix, Italy and Sweden. Six factors with varimax rotation, extracted by principal-axis factoring (Italy) and maximum-likelihood factoring (Sweden); cell entries are loadings $\geq 0.30$ (smaller values suppressed). Variable ordering follows the dominant Italian factor. KMO = 0.832 (IT), 0.808 (SE).}
  \label{fig:ch3-fa-loadings}
\end{figure}

K-means estimation operates on the standardised analytical matrix directly, not on the EFA-extracted factor scores.\footnote{The Swedish factor analysis uses maximum-likelihood extraction. An earlier principal-axis specification produced a Heywood case on the social-integration index (loading 1.11, communality 1.38), signalling that the variable was empirically over-determined within the social factor under PA factoring. The ML specification resolves the case cleanly while preserving comparable fit (RMSEA 0.033 and TLI 0.897 under both extractions; KMO 0.808). The principal-axis alternative and a 30-variable specification that drops social-integration are documented in \S\ref{sec:appx-sens-fa}.} The factor analysis is therefore an interpretative companion to the segmentation, not an intermediate dimensionality-reduction step.

\section{K-means clustering: estimation and choice of \texorpdfstring{$k$}{k}}
\label{sec:ch3-kmeans}

K-means \citep{macqueen1967} with squared-Euclidean distance is the segmentation algorithm, implemented with the Hartigan--Wong update rule \citep{hartiganwong1979} on the country-specific standardised matrix. Given a sample $\{\mathbf{x}_1,\ldots,\mathbf{x}_n\}$ and a target number of clusters $k$, K-means seeks the partition into mutually exclusive sets $\mathcal{C}_1,\ldots,\mathcal{C}_k$ that minimises the within-cluster sum of squares,
\begin{equation}
  \arg\min_{\mathcal{C}_1,\ldots,\mathcal{C}_k}\;
  \mathcal{J}(\mathcal{C}_1,\ldots,\mathcal{C}_k)
  = \sum_{i=1}^{k} \sum_{\mathbf{x} \in \mathcal{C}_i}
    \bigl\|\mathbf{x} - \boldsymbol{\mu}_i\bigr\|^{2},
  \qquad
  \boldsymbol{\mu}_i = \frac{1}{|\mathcal{C}_i|}\sum_{\mathbf{x} \in \mathcal{C}_i}\mathbf{x},
\end{equation}
where $\boldsymbol{\mu}_i$ is the centroid of cluster $i$ and $\|\cdot\|$ is the Euclidean norm. The objective is non-convex; the estimation is therefore run with 100 random initial-centroid configurations and a fixed seed (42), and the configuration with the lowest $\mathcal{J}$ is retained. K-means is preferred to agglomerative alternatives (Ward, complete, average) on the basis of higher within-cluster $R^2$ and silhouette indices reported in \S\ref{sec:ch7-internal-validity}.

The Italian K-means is estimated on $p_{\text{IT}} = 29$ active analytical variables and the Swedish on $p_{\text{SE}} = 31$ (Table~\ref{tab:ch3-sample-sizes}). The two-variable asymmetry reflects the variance-screening step of \S\ref{sec:ch3-preprocessing}. Two cultural-engagement binary indicators (\texttt{ac035d4}, participation in a training course, and \texttt{ac035d7}, participation in a political or community organisation) have zero variance in the Italian sample and are dropped: under squared-Euclidean K-means a zero-variance variable contributes zero to every inter-cluster distance and adds no information. Both indicators retain non-trivial variance in Sweden and are kept there. The cross-country alignment of Chapter~\ref{ch:ch6} operates on a common set of 11 analytical dimensions in which the two excluded indicators do not figure (\S\ref{sec:ch6-matched-pairs}), and the Swedish partition is verified in \S\ref{sec:ch7-internal-validity} to remain structurally invariant when restricted to the 29-variable common subset. The asymmetric input choice therefore does not drive the structure of the Swedish clusters.

The number of clusters $k$ is selected on three criteria. The within-cluster sum of squares scree ($k$ between 2 and 10) places the elbow at $k = 5$ (Italy) and $k = 6$ (Sweden). The Duda--Hart pseudo-$T^2$ statistic on the Ward dendrogram \citep{dudahartstork2001} peaks independently at the same values. Substantive interpretability is decisive at the boundary cases. In Italy, at $k = 4$ the fragile stratum collapses into a single archetype that obscures a substantively important contrast between ``Resigned'' and ``Depressed'' fragile sub-types (\S\ref{sec:ch4-profile-descriptions}); at $k = 6$ the middle stratum splits into two unstable profiles. In Sweden, at $k = 5$ the welfare-attached and digitally active strata collapse together, obscuring a contrast that the cross-country mapping of Chapter~6 relies on; at $k = 7$ the wealthy stratum fragments into unstable sub-profiles. Table~\ref{tab:ch3-internal-validity} reports the internal validity at the chosen $k$.

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/03_data_methods/fig_kselection_scree.png}
  \caption[K-selection scree diagnostic, Italy and Sweden]{K-selection scree diagnostic, Italy and Sweden. Within-cluster $R^2$ (between/total SS) for $k = 2 \ldots 10$ on the standardised analytical matrix. The Italian scree elbows at $k = 5$ and the Swedish at $k = 6$, in agreement with the Duda--Hart pseudo-$T^2$ statistic and substantive interpretability (\S\ref{sec:ch3-kmeans}).}
  \label{fig:ch3-k-scree}
\end{figure}

\begin{table}[!htbp]
  \centering
  \caption[Internal validity of the K-means partition]{Internal validity of the K-means partition at the chosen $k$}
  \label{tab:ch3-internal-validity}
  \begin{tabular}{lrr}
    \toprule
    \textbf{Index} & \textbf{Italy ($k = 5$)} & \textbf{Sweden ($k = 6$)} \\
    \midrule
    Within-cluster $R^2$ (between/total SS) & 0.248 & 0.211 \\
    Mean silhouette width & 0.069 & 0.060 \\
    Duda--Hart pseudo-$T^2$ peak at & $k = 5$ & $k = 6$ \\
    ANOVA F on life satisfaction (external) & 124.6 & 44.3 \\
    \bottomrule
  \end{tabular}
  \par\medskip
  \begin{minipage}{\linewidth}
    \footnotesize\textit{Note.} Within-cluster $R^2$ is between/total sum of squares in the standardised input matrix. Mean silhouette is computed on Euclidean distances. The Duda--Hart pseudo-$T^2$ is the peak value of the statistic on the Ward dendrogram. ANOVA F is computed on the SHARE Wave~9 life-satisfaction item (single-item, eleven-point scale) external to the analytical input set.
  \end{minipage}
\end{table}

The mean silhouette values of 0.069 (Italy) and 0.060 (Sweden) are low in absolute terms, below the 0.250 threshold conventionally taken as indicative of a cohesive cluster structure \citep{kaufman1990}. This is a routine consequence of clustering on a high-dimensional input matrix spanning the six factor subspaces of \S\ref{sec:ch3-fa}, where Euclidean compactness is intrinsically limited and within-cluster heterogeneity remains material along the dimensions on which the partition does not separate. The Duda--Hart pseudo-$T^2$ peak and the substantive interpretability of the profiles therefore carry decisive weight in the choice of $k$, with the full silhouette decomposition by cluster reported in Chapter~7. Given these values, the K-means solution is framed less as a discovery of natural clusters than as a structured segmentation of a continuous multidimensional space: the findings of Chapters~\ref{ch:ch4}--\ref{ch:ch7} are stated as claims about centroid configurations and structural relations among profiles, not about the assignment of marginal individual respondents.

\section{Latent Class Analysis triangulation}
\label{sec:ch3-lca}

The K-means hard assignment is triangulated using Latent Class Analysis fitted on a discretised representation of the input variables: continuous and ordinal items are partitioned at country-level tertile cut-points into three-category ordinal variables; binary items are passed through unchanged. LCA assumes that the joint distribution of the $J$ observed categorical items $\mathbf{Y} = (Y_1,\ldots,Y_J)$ arises from a finite mixture of $K$ latent classes:
\begin{equation}
  \Pr(\mathbf{Y} = \mathbf{y}) = \sum_{k=1}^{K} \pi_k \prod_{j=1}^{J} \Pr(Y_j = y_j \mid C = k),
\end{equation}
where $\pi_k$ is the prior probability of class $k$ ($\sum_k \pi_k = 1$) and $\Pr(Y_j = y_j \mid C = k)$ is the conditional response probability for item $j$ given class membership, under the local-independence assumption. The model is fitted by the EM algorithm \citep{vermuntmagidson2002} with 100 random starts; class membership is then assigned by the modal posterior probability $\arg\max_k \Pr(C = k \mid \mathbf{Y} = \mathbf{y})$. The number of latent classes $K$ is held fixed at the K-means $k$ for each country (so $K = 5$ for Italy and $K = 6$ for Sweden), making the two partitions directly comparable.

Figure~\ref{fig:ch3-lca-convergence} reports the agreement between the K-means hard assignment and the LCA modal-class assignment by profile. Italian profiles converge between 54.6\% (Moderate Isolated) and 95.6\% (Fragile Resigned); Swedish profiles between 49.3\% (Wealthy Digital) and 86.9\% (Connected Wealthy). Random assignment on $k = 5$ Italian classes would yield 20\% per class (17\% on the $k = 6$ Swedish classes); the 50\% reference line is not a canonical statistical cutoff but a deliberately conservative bar, requiring that more than half of a cluster's K-means members fall in the same LCA modal class, far above those chance levels. The corner archetypes (Fragile Resigned and Connected Active in Italy; the corresponding Swedish poles, Fragile and Connected Wealthy) and the Italian Traditional Social profile (78.3\%) converge well above this bar, while the remaining middle-stratum profiles approach it from below.

The convergence pattern itself carries methodological information. The corner archetypes occupy regions of the input space that are dense and well separated from the rest of the population: K-means draws their boundaries by Euclidean compactness around a centroid, LCA recovers them as high-probability latent classes under a categorical mixture model, and the two converge because the underlying configurations are unambiguous. The middle-stratum profiles, Moderate Isolated in Italy and Wealthy Digital in Sweden, sit instead in transitional regions of the latent space, where the cluster boundary is geometrically thin and the assignment of marginal respondents is intrinsically uncertain. The lower convergence (49.3--54.6\%) on these profiles is therefore not a failure of either algorithm but a substantive finding: it identifies the segments for which classification is most sensitive to the modelling choice, and for which any policy or product application should anticipate non-trivial assignment uncertainty. The robustness diagnostics in \S\ref{sec:ch7-cross-method} and \S\ref{sec:ch7-cross-wave} quantify this uncertainty using cross-method assignment stability and bootstrap resampling.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/03_data_methods/fig_kmeans_lca_convergence.png}
  \caption[Cross-method convergence, K-means vs LCA]{Cross-method convergence between K-means and LCA, by profile and country. Bars report the share of K-means cluster members whose LCA modal class coincides with the K-means assignment. The dashed 50\% line is a deliberately conservative reference (a majority of a cluster's members in the same LCA class), not a canonical cutoff, and lies far above the 20\% (Italy) and 17\% (Sweden) levels expected under random assignment.}
  \label{fig:ch3-lca-convergence}
\end{figure}

\section{Methodological commitment: subjective dimensions as constituent inputs}
\label{sec:ch3-subjective-commitment}

A central methodological choice of this thesis is that the five subjective measures listed in \S\ref{sec:ch3-variables} (CASP-12, loneliness, hope for the future, interest, and self-rated probability of survival) enter the K-means and LCA stages on equal footing with the objective indicators, rather than as external validators. The common architecture in older-adult segmentation studies clusters on objective indicators alone and treats subjective dimensions as downstream outcomes \citep{wahrendorfsiegrist2010, jivraj2020}; the architecture adopted here treats them instead as constituent components of late-life experience, with empirical content partially independent of objective state. The position rests on three foundations: the gerontological tradition that frames late life as a phase shaped jointly by objective resources and subjective interpretation \citep{lalivedepinay2005, walker2005active}; the evidence that self-rated health predicts mortality independently of clinically observed health \citep{idlerbenyamini1997, bowling2006}; and the design of the instruments themselves, CASP-12 \citep{hyde2003} ($\alpha \approx 0.84$) and UCLA-3 loneliness \citep{hughes2004} ($\alpha \approx 0.72$), which were constructed to capture substantive components of older-age experience rather than outcomes of objective state.

\label{sec:ch3-subjective-empirical}
The empirical justification for the commitment is a 4D-versus-5D sensitivity test that re-runs the pipeline with the subjective block removed, holding all other parameters constant. External discriminative power is measured by the F-statistic of a one-way ANOVA of life satisfaction (SHARE item AC012, eleven-point scale) on cluster membership; life satisfaction is external to both specifications. The Adjusted Rand Index between the 5D and 4D partitions measures the magnitude of individual reassignment.

\begin{table}[H]
  \centering
  \caption[Subjective-block sensitivity, 4D vs 5D]{Sensitivity to the subjective block: 4D versus 5D specifications}
  \label{tab:ch3-4d-5d-sensitivity}
  \small
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{lrrrr}
    \toprule
    \textbf{Country / Specification} & \textbf{Silhouette} & \textbf{Within $R^2$} & \textbf{ANOVA F} & \textbf{ARI vs 5D} \\
    \midrule
    Italy --- 5D (with subjective) & 0.069 & 0.248 & 124.6 & --- \\
    Italy --- 4D (no subjective) & 0.089 & 0.264 & 73.8 & 0.41 \\
    Italy --- $\Delta$ (4D $-$ 5D) & +0.020 & +0.016 & $-50.8$ ($-41\%$) & \\
    Sweden --- 5D (with subjective) & 0.060 & 0.211 & 44.3 & --- \\
    Sweden --- 4D (no subjective) & 0.079 & 0.229 & 34.6 & 0.52 \\
    Sweden --- $\Delta$ (4D $-$ 5D) & +0.019 & +0.018 & $-9.7$ ($-22\%$) & \\
    \bottomrule
  \end{tabular}
  \par\medskip
  \begin{minipage}{\linewidth}
    \footnotesize\textit{Note.} The 4D specification omits the five subjective measures from the K-means input set; $k$ and all other parameters are held constant. ANOVA F is computed on life satisfaction (SHARE item AC012, eleven-point scale), external to both specifications. ARI vs 5D is the Adjusted Rand Index between the 4D and 5D cluster assignments on the same respondents.
  \end{minipage}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.95\linewidth]{figures/03_data_methods/fig_3_7_NEW.png}
  \caption[Segmentation sensitivity to the subjective block]{Sensitivity of the segmentation to the inclusion of the subjective block. Bar chart of the ANOVA F-statistic on life satisfaction (external outcome) under the 5D specification (with subjective inputs) and the 4D specification (objective inputs only). The 41\% reduction in Italy and 22\% reduction in Sweden quantify the discriminative gain attributable to the subjective dimensions.}
  \label{fig:ch3-4d-5d-discrimination}
\end{figure}

Dropping the subjective block raises internal-validity indices modestly (the routine consequence of clustering on a smaller space) but reduces external discriminative power on life satisfaction by 41\% in Italy and 22\% in Sweden. The 4D--5D ARI is 0.41 in Italy and 0.52 in Sweden, indicating only moderate agreement between the two partitions, well below the 0.80 level conventionally taken as substantive equivalence; the subjective block therefore materially reshapes the partition. The substantive cost in Italy is the collapse of the Fragile Resigned vs Fragile Depressed contrast (\S\ref{sec:ch4-profile-descriptions}) into a single fragile cluster, eliminating an affective distinction the 5D solution preserves. The asymmetry ($-41\%$ vs $-22\%$) is consistent with the cross-country finding that subjective well-being gaps are wider than objective ones (Chapter~\ref{ch:ch6}).

\label{sec:ch3-subjective-circularity}
The circularity objection, that the typology cannot be validly evaluated against subjective outcomes because subjective measures enter as inputs, rests on a predictive framing the analysis does not adopt. The strategy is descriptive: the typology partitions multidimensional configurations of late-life experience with all dimensions on equal footing, rather than predicting subjective well-being from objective state. The 4D--5D test against life satisfaction (an outcome correlated with but not identical to the five subjective inputs, administered in a separate questionnaire block) is therefore closer to construct validity than to a tautology. Two features sharpen the distinction. Life satisfaction is a single global evaluation, whereas the subjective block enters as five specific facets (CASP-12, loneliness, hope, interest, expected survival); the 41\% (Italy) and 22\% (Sweden) drop in its discrimination when the block is removed therefore reflects shared late-life content rather than mechanical overlap. The healthcare-utilisation outcomes, moreover, share no measurement channel with the subjective inputs at all, so the relevant validity argument is the convergent reading across a measurement-adjacent outcome (life satisfaction) and measurement-independent outcomes (healthcare utilisation), not either alone. Genuinely external validation against life satisfaction (\S\ref{sec:ch7-external-validation-lifesat}) and healthcare utilisation (\S\ref{sec:ch7-external-validation-healthcare}) uses outcomes entirely separate from the clustering inputs; further external validations are listed in \S\ref{sec:ch9-future}.

\section{Limitations of the methodological approach}
\label{sec:ch3-limitations}

Two classes of limitations bound the substantive scope. The first is the sample design: SHARE Wave~9 is treated cross-sectionally, precluding causal identification (cross-wave W8--W9 structure is exploited in \S\ref{sec:ch7-cross-wave} only for partition stability); the single multiple-imputation replicate (\S\ref{sec:ch3-preprocessing}) introduces residual sensitivity quantified in \S\ref{sec:ch7-imputation}; the exclusion of proxy interviews biases the typology toward respondents capable of self-reporting on the subjective block; and the two-regime comparison cannot be generalised to corporatist, liberal or post-socialist regimes without separate investigation. The second class concerns modelling: low silhouettes (0.069 IT, 0.060 SE; \S\ref{sec:ch3-kmeans}) reflect the multi-factor input matrix, the conservative $k$ choice favours cross-country comparability over single-index optimality, and the 4D/5D ARI of 0.41 IT and 0.52 SE (Table~\ref{tab:ch3-4d-5d-sensitivity}) indicates only moderate partition agreement, so the subjective block materially reshapes the partition rather than perturbing it at the margins. The robustness battery of Chapter~\ref{ch:ch7} quantifies these sensitivities and bounds the substantive claims to the structural shape of the partition.

\section{Reproducibility protocol}
\label{sec:ch3-reproducibility}

The pipeline is implemented in twenty numbered R scripts and two helper modules in a versioned git repository, building on the analytical scaffold of \citet{piccarreta2025}; each script is parameterised by a single seed (42), all intermediate outputs are written to a versioned directory and indexed by the generating script, and the full pipeline runs in approximately forty minutes on a standard laptop with R-package versions pinned via renv. A companion web application mirroring the analytical findings is documented as an operational deliverable in Chapter~\ref{ch:atlas}.
